import { MultiplierPipe } from './dailysal.pipe';

describe('MultiplierPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplierPipe();
    expect(pipe).toBeTruthy();
  });
});
